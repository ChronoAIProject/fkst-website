# Package roots for engine host conformance.
#
# Relative paths are resolved from the fkst-website repo root. The first root is
# used as the engine conformance project root; keep a local package first so
# external pinned package roots stay external from the engine's point of view.
# fkst-packages:<path> is resolved from the pinned shared fkst-packages checkout.
.fkst/local-packages/site-board
fkst-packages:packages/idle-detector
fkst-website-packages:packages/site-radar
