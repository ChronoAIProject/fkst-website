{
  "name": "fkst-website-site",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "generate": "cd .. && scripts/run.sh generate",
    "build": "npm run generate && eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  }
}
