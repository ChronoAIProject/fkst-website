kind = "package.composed"
name = "site-board"
persistence_class = "composed_judgment_pipeline"

[code]
root = "."

[lib_deps]
# "std" = this repo's own workspace-internal library; "contract" = 库 B's
# published platform core consumed CROSS-REPO by git sha (see [[external_sources]]
# in fkst.workspace.toml). Both resolve through the same per-unit lexical resolver.
libraries = ["std", "contract"]

[event_deps]
packages = ["idle-detector"]
